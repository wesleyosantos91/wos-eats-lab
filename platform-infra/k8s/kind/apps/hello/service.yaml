apiVersion: v1
kind: Service
metadata: { name: hello, namespace: default }
spec:
  type: NodePort
  selector: { app: hello }
  ports:
    - name: http
      port: 80
      targetPort: 5678
      nodePort: 30080